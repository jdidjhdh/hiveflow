# HiveFlow Agent Runtime

ReAct workers, cognitive orchestration, and guardrails built on [hiveflow](https://pypi.org/project/hiveflow/) core.

```bash
pip install hiveflow-agent
```

See [documentation](https://hiveflow.github.io/hiveflow/) for integration guides.
